const Layout = ({
  // children,
  recipes,
}: {
  // children: React.ReactNode;
  recipes: React.ReactNode;
}) => {
  return (
    <>
      {/* {children}  */}
      {recipes}
    </>
  );
};

export default Layout;
